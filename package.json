{
  "name": "state-of-the-union",
  "private": true,
  "type": "module",
  "description": "Static host + WebSocket relay for State of the Union. The game itself has no dependencies; this is only for playing across machines.",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
